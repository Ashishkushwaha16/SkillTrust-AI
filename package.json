{
  "name": "skilltrust-ai",
  "version": "1.0.0",
  "description": "Verified Skill Intelligence Platform - Verify technical skills based on GitHub activity and peer validation",
  "main": "server/server.js",
  "scripts": {
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "build": "cd client && npm run build"
  },
  "keywords": [
    "skill-verification",
    "github-analysis",
    "peer-review",
    "portfolio",
    "developer-matching"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
